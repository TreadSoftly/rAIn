# Argos Lambda runtime deps (Python 3.11 base image)

# ONNX Runtime - Lambda's Python 3.11 image only publishes wheels up to 1.16.x
onnxruntime==1.16.3

# NumPy kept <2.0 to avoid ABI mismatches with ORT 1.16 wheels
numpy>=1.26,<2.0

# Imaging / ML
# Need 8.3+ to access YOLO11/YOLO12 weights; keep ceiling conservative.
ultralytics>=8.3,<8.6
pillow>=10.0,<12.0

# OpenCV 4.12 wheels require NumPy >=2; lock to 4.11 for the NumPy window above
opencv-python-headless==4.11.0.86

# SciPy window aligned with project
scipy>=1.11,<1.15

# Infra
redis>=5.0
boto3>=1.34

# Keep flexible; Py3.11 wheels available
matplotlib>=3.9
