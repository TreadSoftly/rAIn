# Argos Lambda runtime deps (Python 3.11 base image)

# ONNX runtime (Py3.11 compatible; keep <1.24 to avoid ABI surprises)
onnxruntime>=1.22,<1.24

# NumPy window shared with main project; avoid 2.0.* ABI bumps
numpy!=2.0.*,>=1.26,<2.3

# Imaging / ML
pillow>=10.0,<12.0
ultralytics>=8.0

# OpenCV 4.12 wheels require NumPy >=2;
# lock to 4.11 to stay compatible with the NumPy range above.
opencv-python-headless==4.11.0.86

# SciPy window aligned with project
scipy>=1.11,<1.15

# Infra
redis>=5.0
boto3>=1.34

# Keep this flexible so Py3.11 can pick a suitable wheel
matplotlib>=3.9
