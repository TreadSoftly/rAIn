# Resolver anchors used by CI across Linux/macOS/Windows.

# Torch/TorchVision pairs
torch==2.5.1 ; python_version >= "3.12"
torchvision==0.20.1 ; python_version >= "3.12"

torch==2.3.* ; python_version < "3.12"
torchvision==0.18.* ; python_version < "3.12"

# Torchaudio pairs (only installed if you opt into the [audio] extra)
torchaudio==2.5.1 ; python_version >= "3.12"
torchaudio==2.3.* ; python_version < "3.12"

# OpenCV wheel compatible with NumPy<2.3 and rebuilt for NumPy 2
opencv-python-headless==4.11.0.86

# Unified NumPy window; avoid 2.0.* ABI issues
numpy==1.26.4

# Sympy/Scipy pins to satisfy Torch (and avoid onnxslim conflicts)
sympy==1.13.1
scipy==1.13.*

# Keep matplotlib flexible but avoid 3.10+ on Python 3.9 where wheels are spotty
matplotlib>=3.9,<3.10 ; python_version < "3.10"
matplotlib>=3.9 ; python_version >= "3.10"
