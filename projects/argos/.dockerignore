# Ignore everything by default to keep the Docker build context tiny
**

# Keep these (Lambda build needs both dirs)
!.dockerignore
!lambda/
!lambda/**
!panoptes/
!panoptes/**

# But exclude heavy .pt weights; Lambda copies only ONNX
panoptes/model/*.pt
panoptes/model/**/*.pt

# Noise
**/__pycache__/
**/*.pyc
**/.pytest_cache/
venv/
.venv/
dist/
build/
artifacts/
node_modules/
*.log
tests/
