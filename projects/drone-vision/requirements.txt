# ───────── Runtime / CLI ─────────
torch>=2.2
torchvision>=0.17
ultralytics>=8.0

# ONNXRuntime - different wheels for ≤3.9 vs ≥3.10
onnxruntime==1.19.2 ; python_version < "3.10"
onnxruntime>=1.22,<1.24 ; python_version >= "3.10"

# OpenCV 4.12 jumps to NumPy ≥ 2.3 - stay below it for now
opencv-python-headless>=4.10,<4.12

numpy<2
pillow>=11.3
pillow-avif-plugin>=1.4

# Visualisation / analysis helpers
matplotlib
scipy>=1.11
pandas
redis>=5.0             # (optional) heatmap accumulation

# Misc
typer[all]>=0.12
thop

# ───────── Test-time & local -stack ─────────
boto3
moto[all]~=4.2
